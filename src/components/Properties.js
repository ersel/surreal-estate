import styled from 'styled-components';

const PropertiesContainer = styled.div`
  border: 1px solid purple;
`;

const Properties = () => {
  return <PropertiesContainer>Properties Page</PropertiesContainer>;
};

export default Properties;
